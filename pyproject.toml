[project]
name = "python-demo"
version = "0.1.0"
description = "Python learning notebooks — PY100 (basics) and PY200 (applied examples)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.2.0",
    "jupyter>=1.1.1",
    "numpy>=2.0.0",
    "pandas>=2.2.0",
]
